apiVersion: apps/v1
kind: Deployment
metadata:
  name: dynamodb-local
  namespace: retail
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dynamodb-local
  template:
    metadata:
      labels:
        app: dynamodb-local
    spec:
      containers:
      - name: dynamodb-local
        image: amazon/dynamodb-local
        args: ["-jar", "DynamoDBLocal.jar", "-inMemory", "-sharedDb"]
        ports:
        - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: dynamodb-local
  namespace: retail
spec:
  selector:
    app: dynamodb-local
  ports:
  - port: 8000
    targetPort: 8000
  type: ClusterIP
