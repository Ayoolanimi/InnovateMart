# TEMPLATE: Do not commit real secrets. Replace placeholders with secure creation:
# kubectl create secret generic app-secrets -n retail --from-literal=mysql_root_password=REPLACE_ME ...
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: retail
type: Opaque
stringData:
  mysql_root_password: "REPLACE_WITH_SECURE_VALUE"
  postgres_password: "REPLACE_WITH_SECURE_VALUE"
  redis_password: "REPLACE_WITH_SECURE_VALUE"
  rabbitmq_password: "REPLACE_WITH_SECURE_VALUE"
